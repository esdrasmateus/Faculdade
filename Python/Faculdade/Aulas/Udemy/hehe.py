"""import time

# Time é a library pra coisa assim com tempo

class Hora:
    def __init__(self, sec):
        self.sec = sec

# Classe Hora com um atributo

haha = time.localtime()

# time.localtime() cria um "struct" ou um "json" (pra simplificar, se tu n souber oq é um struct) dizendo todo o tempo atual em anos, dias, etc

hehe = Hora(time.mktime(haha))

# time.mktime("struct") retorna o struct só em segundos

print(hehe.sec / 2)

# print dividido por 2 pq ele só quer a partir de meia noite, vulgo, metade de um dia"""

hehe = 6 / 2 * (1+2)
print(hehe)